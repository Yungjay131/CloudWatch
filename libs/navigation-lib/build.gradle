plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

apply from: "${rootProject.projectDir}/dependencies.gradle"

dependencies {
    implementation depends.core_ktx
    implementation depends.appcompat
    api depends.navigator
}